# cpp-sbom — C/C++ (MSVC + CMake + ninja) 環境での SBOM 生成実験

Windows / MSVC2022 / CMake / ninja / Qt 6.11.1 環境で、ビルド成果物 (EXE/DLL) に対する
SPDX 2.3 SBOM を生成し、外部 SBOM (Qt6、受領した独自ライブラリ) と Relation を張る実験プロジェクト。

(構成・使い方は実験完了後に追記)
